    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.product) }}
    </div>
    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.vat) }}
    </div>
    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.quantity) }}
    </div>
    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.unitPrice) }}
    </div>
    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.discount) }}
    </div>
    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.totalHt) }}
    </div>
    <div class="col-lg-3 quotationLine-field">
        {{ form_row(quotationLineField.children.amount) }}
    </div>

    <div class="input-box col-lg-3 quotationLine-field">
        <a href="javacript:void(0)"
           class="delete-link-edit"
           style="float: right; color: red">
            <i class="fa fa-4x fa-trash" data-toggle="tooltip"
               data-placement="top"
               title="{{ 'app.label.product_delete'|trans }}"></i>
        </a>
    </div>
